#include <cs50.h>
#include <stdio.h>
#include <ctype.h>
#include <string.h>

int count_letters(string text)

int main(void)
{
    // Get user input
    string text = get_string("Text: ");

    printf("%s\n", text);
}

int count_letters(string text)
{
    int sum = 0;

    for (int i = 0, n < strlen(text); i < n; i++)
    {
        if (isalpha(text[i]))
        {
            
        }
    }
}